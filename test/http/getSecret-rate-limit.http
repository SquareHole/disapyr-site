### Get Secret (replace {{key}} with a real key)
GET http://localhost:3000/.netlify/functions/getSecret?key={{key}}

### Get Secret (repeat to hit rate limit)
# Repeat this request 60+ times within 5 minutes to trigger rate limiting
GET http://localhost:3000/.netlify/functions/getSecret?key={{key}}

### Expected: After 60 requests, should receive HTTP 429
